name: domain
description: domain
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  computer: ^2.0.0
  freezed_annotation: ^0.14.2
  get_it: ^7.2.0
  redux: ^5.0.0
  redux_thunk: ^0.4.0

  #
  cloud_firestore: ^2.5.1

dev_dependencies:
  json_serializable: ^4.1.3
  freezed: ^0.14.2
  build_runner: ^2.0.4
  build: ^2.0.0
  pedantic: ^1.11.0
  test: ^1.17.9